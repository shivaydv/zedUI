{
  "name": "dot-background",
  "type": "registry:component",
  "title": "Dot Background",
  "description": "A demo showcasing a dot background component.",
  "files": [
    {
      "type": "registry:component",
      "content": "import React from \"react\";\r\n\r\ntype DotBackgroundProps = {\r\n  dotColor?: string; // rgba() or hex\r\n  dotSize?: number; // px\r\n  gap?: number; // px between dots\r\n  mask?: boolean;\r\n  position?: \"absolute\" | \"fixed\"; // NEW\r\n};\r\n\r\nconst DotBackground: React.FC<DotBackgroundProps> = ({\r\n  dotColor = \"var(--color-muted-foreground)\", // Default color\r\n  dotSize = 0.8,\r\n  gap = 32,\r\n  mask = true,\r\n  position = \"fixed\", // Default position\r\n}) => {\r\n  const backgroundImage = `radial-gradient(at ${gap / 4}px ${\r\n    gap / 4\r\n  }px, ${dotColor} ${dotSize}px, transparent 0)`;\r\n\r\n  const maskStyle = mask\r\n    ? {\r\n        maskImage: \"radial-gradient(circle, white 10%, transparent 90%)\",\r\n        WebkitMaskImage: \"radial-gradient(circle, white 10%, transparent 90%)\",\r\n      }\r\n    : {};\r\n\r\n  return (\r\n    <div\r\n    className={`${position} inset-0 pointer-events-none`}\r\n      style={{\r\n        backgroundImage: backgroundImage,\r\n        backgroundSize: `${gap}px ${gap}px`,\r\n        backgroundRepeat: \"repeat\",\r\n        ...maskStyle,\r\n      }}\r\n    />\r\n  );\r\n};\r\n\r\nexport default DotBackground;\r\n",
      "path": "axiomite-ui/dot-background.tsx",
      "target": "axiomite-ui/dot-background.tsx"
    }
  ]
}